services:
  fastapi:
    build: fastapi/
    ports:
      - 8000:8000
    container_name: fastapi

  streamlit:
    build: streamlit/
    depends_on:
      - fastapi
    ports:
      - 8501:8501 
    container_name: streamlit
